let qustions = [
    new Question("At the end of which HTML element does the <script> tag go?", ["bead", "body","html", "It goes in the css"], "body")
    new Question("What is the basic structure for a for loop?", ["for(i=0; i<array.length; i++)", "for(i=== array)", "for (i==1 i++)", "for === false"] "for(i=0; i<array.length; i++)")
]
